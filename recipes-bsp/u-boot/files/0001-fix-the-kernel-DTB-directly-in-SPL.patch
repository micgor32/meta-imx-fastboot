From d9769ad820760fd4d7a674d6090a52b29dd515e6 Mon Sep 17 00:00:00 2001
From: Elena Popa <elena.popa@nxp.com>
Date: Fri, 24 Oct 2025 13:19:57 +0300
Subject: [PATCH] fix the kernel DTB directly in SPL

Upstream-Status: Inappropriate [board-specific Falcon Mode support]

Signed-off-by: Elena Popa <elena.popa@nxp.com>
---
 arch/arm/mach-imx/Makefile |  2 --
 common/spl/Kconfig         |  9 ++++++++-
 common/spl/spl.c           | 11 +++++++++++
 common/spl/spl_fat.c       |  2 ++
 4 files changed, 21 insertions(+), 3 deletions(-)

diff --git a/arch/arm/mach-imx/Makefile b/arch/arm/mach-imx/Makefile
index 76c067fe4dd..95944ecead6 100644
--- a/arch/arm/mach-imx/Makefile
+++ b/arch/arm/mach-imx/Makefile
@@ -23,10 +23,8 @@ obj-y += cpu.o
 endif
 
 ifeq ($(SOC),$(filter $(SOC),imx8m imx9))
-ifneq ($(CONFIG_XPL_BUILD),y)
 obj-y += fdt.o
 endif
-endif
 
 ifeq ($(SOC),$(filter $(SOC),mx5 mx6))
 obj-y	+= cpu.o speed.o
diff --git a/common/spl/Kconfig b/common/spl/Kconfig
index b13ac501052..aa749034c13 100644
--- a/common/spl/Kconfig
+++ b/common/spl/Kconfig
@@ -777,9 +777,16 @@ config SPL_FS_LOAD_KERNEL_NAME
 	  Filename to read to load for the OS kernel when reading from the
 	  filesystem.
 
+config SPL_FS_SKIP_ARGS
+	bool "Skip the kernel device tree from the filesystem"
+	depends on (SPL_FS_EXT4 || SPL_FS_FAT || SPL_FS_SQUASHFS) && SPL_OS_BOOT
+	default n
+	help
+	  Skip the kernel device tree from the filesystem.
+
 config SPL_FS_LOAD_ARGS_NAME
 	string "File to load for the OS kernel argument parameters from the filesystem"
-	depends on (SPL_FS_EXT4 || SPL_FS_FAT || SPL_FS_SQUASHFS) && SPL_OS_BOOT
+	depends on (SPL_FS_EXT4 || SPL_FS_FAT || SPL_FS_SQUASHFS) && SPL_OS_BOOT && !SPL_FS_SKIP_ARGS
 	default "args"
 	help
 	  Filename to read to load for the OS kernel argument parameters from
diff --git a/common/spl/spl.c b/common/spl/spl.c
index 76fd56dfe4b..2e00c36f909 100644
--- a/common/spl/spl.c
+++ b/common/spl/spl.c
@@ -151,6 +151,14 @@ void spl_fixup_fdt(void *fdt_blob)
 		printf(PHASE_PROMPT "arch_fixup_fdt err - %d\n", err);
 		return;
 	}
+
+	if (IS_ENABLED(CONFIG_OF_SYSTEM_SETUP)) {
+		err = ft_system_setup(fdt_blob, gd->bd);
+		if (err) {
+			printf(PHASE_PROMPT "ft_system_setup err - %d\n", err);
+			return;
+		}
+	}
 #endif
 }
 
@@ -797,6 +805,9 @@ void board_init_r(gd_t *dummy1, ulong dummy2)
 			spl_fixup_fdt((void *)SPL_PAYLOAD_ARGS_ADDR);
 		spl_board_prepare_for_linux();
 		jump_to_image = &jump_to_image_linux;
+	} else if (CONFIG_IS_ENABLED(OS_BOOT) && !spl_start_uboot()) {
+		debug("Jumping to ATF -> Kernel\n");
+		spl_fixup_fdt((void *)SPL_PAYLOAD_ARGS_ADDR);
 	} else {
 		debug("Unsupported OS image.. Jumping nevertheless..\n");
 	}
diff --git a/common/spl/spl_fat.c b/common/spl/spl_fat.c
index fce451b7664..c8bf9d4bd02 100644
--- a/common/spl/spl_fat.c
+++ b/common/spl/spl_fat.c
@@ -111,6 +111,7 @@ int spl_load_image_fat_os(struct spl_image_info *spl_image,
 	if (err)
 		return err;
 
+#ifndef CONFIG_SPL_FS_SKIP_ARGS
 #if defined(CONFIG_SPL_ENV_SUPPORT) && defined(CONFIG_SPL_OS_BOOT)
 	file = env_get("falcon_args_file");
 	if (file) {
@@ -147,6 +148,7 @@ defaults:
 #endif
 		return -1;
 	}
+#endif
 
 	return spl_load_image_fat(spl_image, bootdev, block_dev, partition,
 			CONFIG_SPL_FS_LOAD_KERNEL_NAME);
-- 
2.34.1

